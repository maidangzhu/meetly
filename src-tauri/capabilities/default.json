{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the island MVP shell.",
  "windows": ["island", "settings", "voice-overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "clipboard-manager:default",
    "global-shortcut:default",
    "opener:default",
    "updater:default",
    "process:default"
  ]
}
